

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Users</title>

    <style>
        /* Global Styles */
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background: url('https://png.pngtree.com/thumb_back/fw800/background/20231029/pngtree-up-close-background-natural-black-and-gray-charcoal-texture-image_13706119.png') no-repeat center center fixed;
            background-size: cover;
            color: #fff;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {    
            border-radius: 15px;
            padding: 20px 40px;
            background-color: rgba(0, 0, 0, 0.7);
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
            max-width: 1200px;
            width: 90%;
        }

        h2 {
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #ffddc1;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

        th, td {
            padding: 15px;
            text-align: left;
            font-size: 1em;
        }

        th {
            background: rgba(0, 0, 0, 0.5);
            color: #ffddc1;
            text-transform: uppercase;
        }

        tr {
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        tr:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        tr:nth-child(even) {
            background: rgba(255, 255, 255, 0.05);
        }

        a {
            display: block;
            margin: 30px auto 0;
            text-align: center;
            padding: 15px 30px;
            color: #fff;
            background: #ff7e5f;
            text-decoration: none;
            border-radius: 5px;
            font-size: 1.2em;
            transition: all 0.3s ease;
            margin: 0px auto ;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            max-width: 200px;
        }

        a:hover {
            background: #feb47b;
            transform: translateY(-3px);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            h2 {
                font-size: 2em;
            }

            table {
                font-size: 0.9em;
            }

            th, td {
                padding: 10px;
            }
        }

        @media (max-width: 480px) {
            table {
                font-size: 0.8em;
            }

            th, td {
                padding: 8px;
            }

            a {
                font-size: 1em;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>View Users</h2>
        <table>
            <thead>
                <tr>
                    <th>Sr No.</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Password</th>
                    <th>Phone</th>
                    <th>Action</th>

                </tr>
            </thead>
            <tbody>
                <% if (record.length > 0) { %>
                    <% record.map((val, index) => { %>
                        <tr>
                            <td><%= val.id %></td>
                            <td><%= val.name %></td>
                            <td><%= val.email %></td>
                            <td><%= val.password %></td>
                            <td><%= val.phone %></td>
                            <td>
                                <a href="/deleteuser?deleteId=<%= val.id %>">Delete</a>
                            </td>
                            <td>
                                <a href="/edituser?id=<%= val.id %>">Edit</a>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="6" style="text-align: center;">No users found</td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <a href="/add">Add User</a>
    </div>

</body>
</html>

